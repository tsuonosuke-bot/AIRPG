# マスタデータ編集

ゲームを起動せず、`GuildSimulator.Game/Data` 配下のJSONを直接編集できます。

- 冒険者: `adventurers.json`
- スキル: `skills.json`
- 職業とスキルの解禁: `classes.json`
- 装備とレアリティ: `equipment.json`
- 敵とレアドロップ: `enemies.json`
- 消費アイテム: `consumables.json`
- ターン末選択イベント: `choice_events.json`
- 物語の手掛かり: `clues.json`
- ダンジョンへのイベント割当: `dungeons.json`
- ギルド施設: `facilities.json`
- 特性: `traits.json`
- 遺物: `relics.json`（**システムごと凍結中**。下記「遺物は凍結中」を参照）

## 遺物は凍結中

遺物システムは `GameFeatures.RelicsEnabled`（`GuildSimulator.Core/GameFeatures.cs`）で
**止めてあります**。永久バフの入手経路を施設へ一本化するためで、機能を消したわけではありません。

- `relics.json` と、宝箱・ボスドロップに書かれた `relicId` のエントリは**そのまま残してください**。
  消すと復活できなくなります
- 凍結中、遺物エントリは抽選候補から外れます。除外したあとに重みを取り直すので、
  同じテーブルにある他の中身の出やすさの比率は変わりません
- したがって、**遺物エントリしか無いテーブル**は凍結中に空振りします。
  `dungeons.json` の `treasureTable` と `quests.json` の `bossDrops` には、
  遺物以外の中身も1件は入れておいてください
- 復活させるときはフラグを `true` に戻すだけです（詳細は [README.md](README.md) の「機能フラグ」）

冒険者には戦闘能力に加えて、`background`、`personality`、`motivation`、
`specialty`、`fear`、`creed`、`selfIntroduction` を設定できます。

`gender` は `Unspecified` / `Male` / `Female` のいずれかです（既定は `Unspecified`）。

物語クエストでは `isStoryQuest` を `true` にし、次のIDリストで進行順を定義します。

- `requiredQuestIds`: 掲示に必要な完了済みクエスト
- `requiredClueIds`: 掲示に必要な発見済み手掛かり
- `grantedClueIds`: 正規クリア時に発見する手掛かり
- `storyArcId`: 同じ物語章として調査記録へまとめるID
- `storyArcTitle`: 調査記録に表示する章名

掲示可能になった物語クエストは通常依頼の抽選へ混ぜず、**物語専用枠へ1件を継続掲示**します。
受注されるまで7ターンの掲示期限では消えません。

物語上の発見や決断は、`fixedEvents` に `type: 7`（`ForceChoice`）と
`choiceEventId` を設定して、クエスト固有のエリアで必ず発生させます。
未解決の固定選択がある間は、討伐・踏破・採取目標を達成していても帰還完了にはなりません。

`choice_events.json` の選択肢には、通常の `effectType` と同時に次を設定できます。

- `grantedClueId`: 選んだ時点で発見し、調査記録へ発見順で保存する手掛かり
- `storyBranchId`: 永続保存する物語の結末・分岐
- `storyOutcomeText`: 調査記録へ残す、その決断による世界の変化

現在の青い鉱石事件では、最終的に封印・知識公開・交易のいずれかを選びます。
結末は廃坑と旧市街の罠、経験値、報酬・遭遇率へ継続して反映されます。

### 天城探索記（以下、物語の真相を含む）

天城は最初から既知の土地として掲示しません。Eランク依頼「流れる森・深部の偵察」を完了した時、
風が濃霧を裂き、地図にない白い巨塔が初めて姿を現します。雲へ消える姿から調査隊が
仮称「天城（あましろ）」を付けますが、この時点では用途も高さも、上に何があるかも不明です。

プレイヤーへの情報開示は、次の順序で進めます。

1. 森深部: 濃霧の先に、終端の見えない未知の巨塔を発見する。
2. 天城低層: 旧隊商路が塔内を上っていたと知るが、送り状の最終目的地は削られている。
3. 天城中層: 動甲冑とゴーレムの命令を、簒奪者の古龍が書き換えたと突き止める。
4. 天城高層: 小型から大型までの飛竜を越え、雲上に浮かぶ海「ミドルオーシャン」を初めて見る。
5. 天城最上層: 古龍の守護機から昇潮鍵を取り戻し、空の海への道を開く。
6. ミドルオーシャン: 沈没船の目録から、その先に高度文明「天界」があり、かつて交易していたと知る。
7. 天界門: 門が天界側の結界で閉ざされていることを初めて知り、今後の方針を決める。

設定上の真相では、天城は地上と天界を結んだ巨大な垂直交易路です。古龍は天城を簒奪して
昇潮鍵を奪い、守備機へ「上る者を拒め」と命じました。その結果、天界側の非常結界も作動し、
交流は途絶えています。雲上のミドルオーシャンには空魚・空ザメ・空鯨などが生息します。
古龍本人と天界内部は後続章のために残し、この章では古龍が残した守護機と結界の真相までを扱います。

アイテムは宝箱で手に入ります。道中では未開封のまま持ち運び、**帰還後に開けて中身を抽選します**
（全滅すると開ける前に失います）。宝箱は2種類です。

- 道中の宝箱: 中身は `dungeons.json` の `treasureTable` から1件。`weight` で出やすさを決めます。
  このダンジョンで拾えるアイテムはすべてここに書きます。**1割の確率で空っぽ**になります
  （`QuestRewardService.EmptyChestRate`）。所持済みの遺物は抽選対象から外れます。
  **Gold エントリの重みの合計は、同じ依頼ランクで引ける中身全体の2割以下**に収めます。
  宝箱を開ける楽しみは装備・消耗品・スキルの側にあり、ゴールドばかり出ると
  「開けた意味がない箱」になってしまうためです。
  入手経路は道中の宝箱イベントと、`choice_events.json` の選択肢に
  `"effectType": "Treasure"` を置いたもの（`value` 個ぶん渡す）です。
- ボスの宝箱: ボス撃破で手に入り、中身は `quests.json` の `bossDrops`。
  エントリごとに `chance`（0より大きく1以下）で抽選し、**空っぽ抽選は受けません**。
  物語進行に必要で落とすと詰むものは、クエスト側の `bossDropsAreGuaranteed` を `true` にすると
  `chance` も無視して全て確定します。

宝箱以外に、敵のレアドロップ（`enemies.json` の `dropTable`）と選択イベントの拾い物は
中身が分かっている戦利品としてそのまま持ち帰ります。

### 敵ドロップの確率 (`enemies.json` の `dropTable`)

`chance` は**品物のレアリティ**と、**その敵が1クエストで何回抽選されるか**の2軸で決めます。
通常個体は同じクエストで何度も倒すので低く、ボス専用個体（`dungeons.json` の
`encounterTable` に載らず、クエストのボスとしてしか出てこない敵）は1回しか判定が起きないので
高くします。とくにボスの Unique・Legend は「そのボスを倒した証」なので別格に高くします。

| レアリティ | 通常個体 | ボス専用個体 |
| --- | --- | --- |
| Uncommon | 1.0% (`0.01`) | 2.5% (`0.025`) |
| Rare | 0.6% (`0.006`) | 1.5% (`0.015`) |
| Unique | 0.4% (`0.004`) | 10% (`0.1`) |
| Legend | 0.2% (`0.002`) | 4% (`0.04`) |
| 消耗品 | 4% (`0.04`) | 6% (`0.06`) |

群れで出る敵（殺人蜂・洞窟コウモリなど）は1戦闘で4回抽選されるので、
同じ `chance` でも体感は4倍になります。これは織り込み済みで、
低い率のまま「群れを狩っていると稀に出る」形にしています。

数値は `GuildSimulator.Tests/EnemyDropVarietyTests.cs` で固定しています。
表を変えるときはそちらも一緒に直してください。

## 採取クエスト

採取クエスト（`gatherTargetCount` > 0）の採取判定は、ダンジョンの `eventTable` とは
**別枠**です。エリアごとに `gatherChance` で判定し、当たれば同じエリアの戦闘や宝箱と
並行して採取が進みます（イベントを置き換えません）。目標数に届いた時点で判定は止まります。
ボスのエリアでは採取判定は起きません。

**採取クエストの達成条件は素材が揃ったかどうかだけ**です。予定エリアを踏破しても手ぶらなら
クリアではありません（`QuestRun.ReachedGoal`）。

### 予定エリアを使い切ったとき

パーティは勝手に引き返さず、**現地に留まって指示を待ちます**。プレイヤーの二択です
（`QuestManager.ResolveGatherDecision`）。

- **捜索を続けさせる**: 行程が `phasesPerTurn` ぶん伸びる。**回数制限はなく、届くまで何度でも聞く**
- **引き上げさせる**: `GatherTargetMissed` で撤退。基本報酬と経験値は
  `QuestRewardService.RetreatRewardRate`（=40%）まで減り、ギルドポイントはなし

指示待ちの間はそのクエストの進行が止まり、**ターンも進められません**（未解決の選択イベントと同じ扱い）。

延長に直接のペナルティはありません。**代価は「もう1ターン帰ってこない」ことそのもの**です。

- 余分に1ターンぶんの維持費を払う
- 踏み足すエリアでも戦闘・罠・士気の消耗は起きる。粘るほど全滅と士気崩壊が近づく
- 抱えている未開封の宝箱も、全滅すれば一緒に失う

つまり「あと少しだから粘る」「損切りして帰す」を、士気とHPの残りを見て決めることになります。
延長が無限に許されていても、**ダンジョンの側が勝手に代価を取り立てる**ので歯止めは要りません。

### 数値の決め方

延長という逃げ道があるので、**ここで決めるのは失敗率ではなく「予定どおりに帰れる率」**です。
高すぎれば二択が一度も出ず、低すぎれば予定が予定になりません。

**ランクが上がるほど下げます。** 序盤は素直に採って帰れて、奥へ行くほど
「あと1ターン粘るか、損切りして帰すか」を突きつけられる頻度が上がります。

| ランク | F | E | D | C | B | A | S |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 予定内で達成 | 76〜84% | 72〜80% | 68〜76% | 65〜73% | 63〜71% | 61〜69% | 60〜68% |
| 延長判断が出る | 約1/5 | 約1/4 | 約2/7 | 約1/3 | 約1/3 | 約4/11 | 約3/8 |

達成率は `RankBandTable.GatherSuccessPercent` が**厳密に計算**します（エリアごとの畳み込み）。
「期待量が目標の何倍か」という近似では足りません。同じ期待量でも「毎エリア1〜2個」と
「たまに大量」ではブレの幅がまるで違い、達成率が10ポイント以上ずれるためです。
`MasterBandChecker` がこの計算値を上の帯と突き合わせます。

```
採取できるエリア数 = totalPhases −（ボス付きなら 1）
```

加えて `gatherChance ≥ 0.65` を守ります。これは達成率とは別の、**手触りのための下限**です。
判定回数が少ないと、同じ達成率でも「数回の当たり外れで決まった」感触になり、
少しずつ集まっていく手応えが消えます。**素材の希少さは `gatherChance` ではなく
1回の採取量の幅（`gatherMin`／`gatherMax`）で表してください。**
`quest_clover_search` は1回に1〜2枚しか見つからない代わりに目標数が9枚で、
「探し歩けば見つかるが、一度にはまとまらない」希少さになっています。

数値を詰めるときは `gatherChance` と1回の採取量を先に決めて素材の性格を作り、
**`gatherTargetCount` だけを動かして達成率を帯に入れます**。目標数1個の増減で
達成率はおよそ5〜7ポイント動くので、帯（8ポイント幅）にはたいてい1〜2通りしか収まりません。

### 現在の採取クエスト

| クエスト | ランク | `totalPhases` | `gatherChance` | 1回の採取量 | 目標数 | 予定内で達成 | 延長1回後 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `quest_herb` | F | 10 | 0.8 | 1〜4 | 17 | 78.0% | 99.4% |
| `quest_moonflower` | F | 10 | 0.7 | 1〜4 | 14 | 80.0% | 98.9% |
| `quest_investigate_woods` | F | 10 | 0.75 | 2〜4 | 19 | 80.3% | 99.5% |
| `quest_wild_berries` | F | 10 | 0.75 | 1〜4 | 15 | 82.0% | 99.3% |
| `quest_clover_search` | F | 10 | 0.7 | 1〜2 | 9 | 78.3% | 99.0% |
| `quest_iron_ore` | D | 20 | 0.7 | 3〜6 | 57 | 74.0% | 97.4% |
| `quest_cave_mushroom` | D | 17 | 0.7 | 2〜5 | 37 | 74.9% | 97.7% |
| `quest_old_city_relic` | D | 18 | 0.7 | 2〜5 | 40 | 72.0% | 97.0% |
| `quest_grave_moss` | D | 18 | 0.7 | 2〜4 | 34 | 74.6% | 97.7% |

「延長1回後」は、二択で続行を選んで `phasesPerTurn` ぶん粘ったときの累計達成率です。
**一度延ばせばほぼ確実に届きます。** 延長は「届くかどうか」の賭けではなく、
**1ターンぶんの維持費と道中のリスクを払うかどうか**の判断になるよう、ここを高く保ちます。

`gatherGoldPerItem` は目標数を超えたぶんの買取価格です。目標に届いた時点で採取は止まるので、
実際に乗るのは最後の1回の採取が目標を追い越したぶん（最大 `gatherMax − 1` 個）だけです。

## 戦闘ダメージ

戦闘の解決は **Caves of Qud** の方式です。攻撃は「命中判定」と「貫通判定」の二段構えで、
ダメージの大きさはダメージダイスそのものではなく **何回貫通したか** で決まります。

```
0. 手番あたりの攻撃回数
   右手: 1 + 連撃(extraAttacks)。追撃はn回目ごとにPVが2ずつ下がる（FOLLOW_UP_PV_PENALTY）
   左手: 武器を持っていれば確率で1回。発動率 = 15(OFF_HAND_BASE_CHANCE)
         + 左手武器のoffHandBonus + スキルのoffHandChance
         左手の追撃はPVの減衰を受けない（連撃とは別枠）

1. 命中判定
   1d20 + 命中補正 > 相手のDV  なら命中
   DV = 6 + 敏捷modifier + 装備・スキル由来のDV補正
   出目が (20 - 会心域critRange) 以上なら会心。出目1だけは会心にならず必ず外れる

2. 貫通判定
   PV = 武器の基礎PV + min(筋力modifier, 武器ごとの上限) + 装備・スキル由来のPV補正
        （魔法は 知力modifier を使う）
   AV = 体格modifier + 装備・スキル由来のAV補正 - 装甲貫通(armorPierce) - 蓄積した装甲破壊
        + 盾の受けに成功したときだけ blockAv
        （魔法は mAV。精神modifier + mAV補正。装甲貫通も装甲破壊も盾も効かない）

   (1d10-2) + PV > AV  の試行を3回で1セット
     ├ 1回も上回らない → 装甲に弾かれた（ダメージ0）
     ├ 1〜2回上回った  → 1貫通してそこで終了
     └ 3回とも上回った → 1貫通し、PVを2下げて次のセットへ（以降くり返し）

3. ダメージ
   貫通回数ぶんだけ武器のダメージダイスを振って合計する
   貫通していれば、相手のAVを装甲破壊(armorShred)のぶんだけ削る（戦闘が終わるまで戻らない）
```

- **能力値は直接ダメージに乗りません。** 筋力（魔法は知力）はPVに、敏捷はDVと命中に、
  体格（魔法は精神）はAVに変わります。ダメージを増やすのは貫通回数と武器ダイスだけです
- 能力値modifierは `(能力値 - 8) / 2` の切り捨てです。Qudの `(能力値-16)/2` を、
  本作の能力値レンジ（Lv1で4〜14）に合わせて基準点を8に縮尺したものです
- 貫通ダイスは `1d10-2`（-1〜8）で、**最大の出目が出るたびに振り足して加算します**（爆発ダイス）。
  上振れが青天井なので、AVが高くても薄い可能性で貫通の目が残ります
- 貫通は1セットごとにPVが2ずつ減っていくため、どれだけ格上でも必ず有限回で止まります
- 物理か魔法かは能力値の大小ではなく武器で決まります。武器の `attackKind` が `Magic` なら魔法です
- 会心（1d20の**素の出目**が20）はDVに関わらず命中し、PVが+1され、
  1回も抜けなかった場合でも最低1貫通は通ります
- **守り手の隙**（`incomingCritChancePercent`）は、当たった一撃を会心へ引き上げます。
  会心域(`critRange`)は出目そのものを動かすので**5%刻みでしか表せません**。
  「2%だけ会心を受けやすい」のような細かい代償はこちらで書きます。
  外れた攻撃は会心になりませんし、すでに会心の一撃も変わりません
  （`QudCombat.RaiseToCritical`）
- 貫通できなければダメージは0です。最低保証はありません。AVへの投資がそのまま被弾の遮断になります

関係するマスタの項目は次のとおりです。

- `equipment.json` の `attackKind`: `Physical` / `Magic` / `Heal`。武器が何を撃つかを決めます
- `equipment.json` の `damageDice`: 貫通1回につき振るダメージダイス（`1d6`、`2d4+1` など）。
  未設定の武器は素手扱いの `1d2` になります
- `equipment.json` の `basePv`: 武器そのものの貫通値。素材の等級がそのままPVの差になります（標準は4）
- `equipment.json` の `maxStatBonus`: PVに上乗せできる能力値modifier（筋力／知力）の上限。
  軽い得物ほど小さく、重い得物ほど大きくして「短剣に腕力を乗せきれない」を表現します。
  上限は武器クラスごとの固定値とし、Tierでは変えません（Tier差は `basePv` とダイスが表現します）。
  最軽量（短剣・風）5／標準（剣・弓・投石・水）6／長柄（槍・地）7／重量級（斧・火・闇）8。
  クラス間を1点刻みにすると、主能力がどれだけ伸びてもクラス間の差は最大3点（＝PV3点）で頭打ちになります。
  重い得物を無制限にすると主能力が伸びるほど差が青天井に開くため、重量級にも上限を置いています。
  なお上限を超えた主能力は**行き場がありません**。能力値がダメージに直接乗らない方式なので、
  伸ばしすぎた筋力を受け止める先（旧方式のダメージ・ボーナス）は存在しません。
  回復魔法は貫通判定を通らないため、光属性の `maxStatBonus` と `damageDice` は使われません
- `equipment.json` 防具の `bonus`: `av` / `mav` / `dv` で表します。重い鎧ほどAVが高く、DVを削ります
- `equipment.json` の `armorType`: `Cloth`(0) / `LightArmor`(1) / `Plate`(2) / `Null`(3)。
  スキルの `requiredArmorType` はこれと突き合わせます。**列挙順がJSONの数値そのものなので並べ替え禁止**です
- `equipment.json` の `allowedSlots`: 装備できるスロット（`RightHand` / `LeftHand` / `Head` / `Body` / `Accessory`）。
  **未指定の防具はすべて `Body` 扱いになる**ため、頭防具には `["Head"]` の明示が必須です
- `enemies.json` の `naturalDamageDice`: 武器を持たない敵の牙・爪・体当たりのダイス
- `enemies.json` の `naturalPv`: 素手の敵の牙・爪そのもののPV。武器持ちの敵は武器の `basePv` が優先されます
- `enemies.json` の `naturalAv` / `naturalMav`: 甲殻・毛皮など、防具を着ていなくても持っている装甲。
  防具の上にも常に加算されます（鱗の上に鎧を着ている、の表現）

なお **AV / DV / PV は1点の重みが大きい小さな整数** なので、スキルや遺物の効果はすべて
加算（`add`）で表します。倍率（`mul`）が効くのは HP・士気(san)・回復量(heal) だけです。

## 武器クラスの個性

`basePv` / `damageDice` / `maxStatBonus` / 命中補正だけでは「その得物にしかできないこと」を
表せないため、武器には次の5つの項目があります。**同じ `weaponType` かつ同じ持ち手（片手/両手）なら
全Tierで同じ値**にします（`maxStatBonus` と同じ扱いで、Tier差は `basePv` とダメージダイスだけが
表します）。`MasterValidator` が `(weaponType, isTwoHanded)` ごとの一致を検査します。

- `equipment.json` の `armorPierce`: 命中時に相手のAVをこの値だけ無視する（**槍**）。
  PVを上げるのと違い、硬い相手にだけ効き、素肌の相手には何も起きません。効果はその一撃かぎりです
- `equipment.json` の `armorShred`: 貫通した攻撃1回につき相手のAVをこの値だけ削る（**斧**）。
  削れた装甲は戦闘が終わるまで戻らず、**味方全員の攻撃**に効きます。
  1体につき合計 `QudCombat.MAX_ARMOR_SHRED`(=3) まで。物理AVのみで、mAVは割れません
- `equipment.json` の `critRange`: 会心になる1d20の出目の幅（**短剣**）。0なら20のみ、2なら18〜20。
  上限は `QudCombat.MAX_CRIT_RANGE`(=5)。出目1は補正でも会心域でも救われません
- `equipment.json` の `extraAttacks`: 1手番あたりの追撃回数（**短剣**）。
  追撃はn回目ごとにPVが `QudCombat.FOLLOW_UP_PV_PENALTY`(=2) ずつ下がるため、
  手数がそのまま火力の掛け算にはなりません
- `equipment.json` の `offHandBonus`: 左手に持ったときの発動率への加算（%、**短剣**）。
  右手に持っているあいだは使われません

同じ項目は `StatBlock` にもあるので、スキルや遺物の `add` からも伸ばせます
（例: 斧マスタリー・極の `"armorShred": 1`、二刀流スキルの `"offHandChance": 35`）。

近接4種は「同じTierなら総合火力がほぼ揃い、得意なAV帯だけが違う」ように調整してあります。

| 武器種 | 命中 | `maxStatBonus` | 個性 | 強い相手 |
| --- | --- | --- | --- | --- |
| 剣 (0) | +1 | 6 | なし | 相手を選ばない。序盤に強い |
| 短剣 (11) | +3 | 5 | `critRange` 2 / `extraAttacks` 1 | 装甲の薄い相手。硬い相手には弾かれる |
| 槍 (2) | -2 | 7 | `armorPierce` 2 | 鎧を着た相手。素肌の相手には旨みがない |
| 斧 (1) | -4 | 8 | `armorShred` 2 | 硬い相手を仲間ごと有利にする。長引くほど効く |

弓は後衛から撃っても命中ペナルティを受けない点が個性で、追加項目は使いません。
魔法はAVではなくmAVと突き合わせる点が個性なので、こちらも追加項目は使いません。

## 右手・左手・両手武器・盾

- `equipment.json` の `isTwoHanded`: 両手で構える武器。左手が塞がるので盾も二刀流も併用できません。
  **弓と魔法（回復杖を含む）は必ず `true`** にします（`MasterValidator` が検査）。
  大剣・大斧・長槍は片手版と**同じ `weaponType`** なので、既存のマスタリーがそのまま効きます。
  そのぶん `basePv` ・ダメージダイス・`maxStatBonus` を1段上げ、重量も倍近くにします
- `equipment.json` の `type` に `Shield`(3) が増えました。盾は `allowedSlots: ["LeftHand"]` 固定です
- `equipment.json` の `blockChance`: 盾で受け止められる確率（%）
- `equipment.json` の `blockAv`: **受けに成功した攻撃にだけ**乗る装甲値。
  `bonus` の `av` は常時加算されるので、**盾の装甲をそちらに書いてはいけません**
  （「構えていなくても硬い」ことになってしまう）。`MasterValidator` が弾きます

左手に握った**武器**の `bonus` は乗りません（短剣を2本持って命中を二重取りできないため）。
攻撃にだけ使われます。盾は防具なので `bonus` は通常どおり乗りますが、装甲だけは受け成功時のみです。
重量はどちらも常に積載へ加算されるので、二刀流も盾も筋力・体格の裏づけが要ります。

| 装備 | 左手 | 特徴 |
| --- | --- | --- |
| 片手武器 | 空き / 武器 / 盾 | 二刀流も盾も選べる |
| 両手武器（大剣・大斧・長槍） | 塞がる | 数値は1段上、重い |
| 弓・魔法・回復杖 | 塞がる | 後衛から撃てる（魔法はmAVと戦う） |

バランスは `GuildSimulator.Tests/WeaponIdentityTests` が実際の `BattleResolver` を回して測ります。
数値を触ったら次で表を確認できます。

```powershell
dotnet test --filter WeaponIdentity --logger "console;verbosity=detailed"
```

## 認定ランク (F〜S)

冒険者・クエスト・ギルドの3つのランクは**同じ物差し**に乗っていて、互いに突き合わせます。

- クエストランク ＞ ギルドランク → そのクエストは掲示されない
- クエストランクが冒険者の適正帯から外れる → クラス習熟度が入らない

JSONとセーブデータには **1〜7 の整数** で入れます。表示は `Rank.Label()` を通した文字です。

| 保存値 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 表記 | F | E | D | C | B | A | S |
| レベル上限 | 5 | 10 | 16 | 24 | 32 | 40 | 50 |
| 職業ごとの習熟度上限 | 700 | 1,800 | 3,300 | 5,200 | 6,300 | 7,500 | 8,800 |

レベルと職業ごとの習熟度は、現在の冒険者ランクの上限を超えて増えません。
上限中の余剰経験値は持ち越さず、昇格すると次の上限まで成長を再開します。
上限値は `Rank.LevelCap()` / `Rank.MasteryCap()` を唯一の定義として参照してください。

対象は `quests.json` の `rank`、`adventurers.json` の `defaultRank` / `recruitGuildRank`、
`facilities.json` の `requiredGuildRank`、`enemies.json` の `threat` です。範囲外の値は端に丸められます
（`RankTests` がマスタ全体を検査します）。**プレイヤーに見せる文字列で数値をそのまま出さないでください。**

現在のマスタデータは F〜S の通常クエストと、F→E から A→S までの昇格試験を備えています。
通常遭遇は原則としてクエストランク+1までに抑え、格上は深度帯で分離します。受注前表示では
通常遭遇の中心脅威と最大脅威（遭遇見込み）を分け、確定ボスは脅威度と編成人数を別表示します。

### 冒険者名簿のランク配分

冒険者39人は F 20人 / E 13人 / D 6人に分けてあります。**序盤に選べる顔ぶれを絞り、
ギルドランクが上がるたびに新しい人材が名簿へ加わる**ようにするためです。とくに魔導士は
F帯を水（ミレイユ）・土（ドラン）の2人だけにして、風・闇と、
エルフの水・土はE帯以降で解禁します。

**F帯に炎魔導士はいません。** 炎はマルシルだけが担っていて、彼女をE帯へ移したときに
F帯から抜けました。序盤で炎を使いたくなったら、F帯のCommonとして新しい炎魔導士を
足すことになります（マルシルをF帯へ戻すと、地下大迷宮の5人が揃わなくなります）。

上位ランクの冒険者を足すときは、次を揃えてください。

- `defaultRank` と `recruitGuildRank` を同じ値にする（そのギルドランクに達するまで候補に出ない）
- `defaultLevel` を「ランク帯の物差し」の冒険者レベル帯に収める（E=6〜10、D=11〜16）
- 能力値は Lv1 の素質に `defaultLevel − 1` 点を足す。振り分けはレベルアップと同じ
  「種族の成長率＋職業の成長率＋`AdventurerData.BaseGrowthWeight`」の比で、
  体力・精神・筋力・敏捷・知力の5つにだけ配る（体格と容姿はレベルでは伸びない）
- `recruitWeight` と `rarity` は `RecruitmentSystem.DefaultWeightForGuildRank` と
  `MasterLoader.DefaultAdventurerRarity` の対応に合わせる（E=60/Uncommon、D=40/Rare）

### 素質とレアリティ上乗せ

強さは**素質（生まれつき）とレベル（積んだ場数）の2階建て**です。名簿に書く能力値は
この2つの足し算になります。

```
能力値の合計 = 素質 + (defaultLevel − 1)      ※レベル1につき1点（AdventurerData.StatPointsPerLevel）
素質         = 65 + レアリティが1段上がるごとに +5
```

**素質は7能力（体力・精神・筋力・敏捷・知力・体格・容姿）の合計で数えます。**
体格と容姿はレベルで伸びないぶん、最初から素質の一部として配られているためです。
5能力だけで比べると、体格19を取ったグリムが「弱い」ことになってしまいます
（5能力44・7合計71。素質としてはロビンやダグとまったく同じ65）。

| レアリティ | 素質（7能力・Lv1換算） | 名簿の人数 |
| --- | --- | ---: |
| Common | 65（容姿にゆとりを持たせて67まで） | 14 |
| Uncommon | 70 | 10 |
| Rare | 75 | 11 |
| Unique | 80 | 4 |
| Legend | 85 | 0 |

**素質にランクは関係ありません。** D帯のRareもF帯のRareも同じ75です。帯が決めるのは
`defaultLevel` の範囲だけで、素質はレアリティだけで決まります。

段ごとに +5 で、頭打ちはありません。希少な人材ほど生まれつき恵まれている、という
一本の物差しです。**強さの2階建て（素質＋レベル）のうち、レアリティは素質の側だけを動かします。**
希少さはこのほかに `recruitWeight`（出にくさ）と雇入れ費の上乗せにも表れます。

重みも必ず下げます。`MasterLoader.DefaultAdventurerRarity` の対応（Rare=26〜45）に収まり、
かつ同じ帯の既定（F=100、E=60）より低い値にします。**帯の顔ぶれの中で目に見えて出にくいこと**が
レアリティの中身なので、重みを下げずにレアリティだけ上げてはいけません。
マルシルもミカヤも `recruitWeight` は30です。

素質70はE帯のUncommon（65〜66）を4〜5点上回り、7能力の合計ではD帯のRare（Lv11〜13で74〜78）に
並びます。これは意図した形です。**素質で上回り、レベルと成長の余地では負ける**のが
「一段下の帯に紛れ込んだレア」の姿で、ミカヤはE帯なのでレベル上限も10で頭打ちになります
（`Rank.LevelCap()`。D帯のRareは16まで伸びます）。

## ランク帯の物差し

F〜S の7帯それぞれについて、**マスタに書く数値がどのくらいの大きさになるか**を決めた表です。
個々の値を詰める前にここを見て、帯からはみ出していないかを確かめます。
`MasterValidator` はこの表を検査に使います。

### 帯の対応

| ランク | F | E | D | C | B | A | S |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 冒険者レベル | 1〜5 | 6〜10 | 11〜16 | 17〜24 | 25〜32 | 33〜40 | 41〜50 |
| 敵の `threat` | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
| 買える `shopTier` | 1 | 2 | 3 | 3 | 3 | 3 | 3 |
| 拾える `shopTier` | 1 | 2 | 3 | 4 | 4 | 5 | 5 |
| 舞台 | 草原・森 | 森・天城低層・草原深部 | 廃坑・墓所・森深部・旧市街 | 天城中層 | 天城高層 | 天城最上層 | ミドルオーシャン |

**商店で買えるのは `shopTier` 3 までです。** 4以上はダンジョンの宝箱・ボスドロップ専用にします。
天城は `encounterTable` の `minPhase` / `maxPhase` で、低層の旧隊商路、中層の機兵、
高層の飛竜へ生態を切り替えます。ミドルオーシャンは天城編の後半で解禁する独立ダンジョンです。

### 進行の見積り

| ランク | F | E | D | C | B | A | S |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 帯を抜けるのに遊ぶ本数 | 5 | 6 | 7 | 8 | 9 | 10 | 10 |
| `totalPhases` | 10 | 15 | 20 | 25 | 30 | 35 | 40 |
| 帯の消費ターン | 10 | 18 | 28 | 40 | 54 | 70 | 80 |
| 到達ターン（累計） | 10 | 28 | 56 | **96** | 150 | 220 | 300 |

`phasesPerTurn` は全帯 5 です。**B到達がおよそ96ターン**で、これが設計の基準線になります。
用意する本数は「遊ぶ本数」より多くてかまいません（掲示板で選べるようにするため）。
採取クエストは各帯の 1/5 とし、残りはボス付きにします。採取の数値の決め方は
前掲「採取クエスト」を参照してください（予定エリア内に目標へ届く確率を
F の76〜84%からSの60〜68%まで、ランクが上がるほど下げます）。

### 経験値

戦闘の経験値とクエスト完了報酬の `rewardExp` は、どちらも
**経験値を受け取れる参加者の人数で頭割り**されます（`QuestProgressor.cs` / `QuestRewardService.cs`）。
戦闘経験値を全員へ満額配ると、人数ぶん経験値総量が増え、1本の依頼で複数レベルを
飛び越えやすくなるためです。
同時編成の基準人数は **F=3人、E〜D=4人、C〜B=5人、A〜S=6人** です。
人数が少ない帯ほど1人あたりの取り分が増えるため、下表の報酬値はこの段階解放を含めて
Balance Labで実測します（遭遇率25%・1戦闘あたり敵3体が基準）。

| ランク | F | E | D | C | B | A | S |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 帯を抜けるのに要るexp（1人） | 100 | 225 | 435 | 860 | 1,180 | 1,500 | 2,070 |
| 1本で1人が得るexp（目安） | 20 | 38 | 62 | 108 | 131 | 150 | 207 |
| 敵1体の `exp` | 3〜6 | 7〜10 | 11〜16 | 16〜24 | 28〜30 | 34〜38 | 45〜70 |
| クエストの `rewardExp` | 10〜45 | 40〜100 | 100〜150 | 150〜220 | 220〜300 | 300〜400 | 400〜550 |

次レベルに要るexpは `10 + (Lv−1) × 5` です（システム側の固定値）。

| 到達Lv | 5 | 10 | 16 | 24 | 32 | 40 | 50 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 累計exp | 70 | 270 | 675 | 1,495 | 2,635 | 4,095 | 6,370 |

### ゴールド

維持費は `ギルド基本8 + Σ(冒険者Lv × 1 + (冒険者ランク − 1) × 6) + 施設の維持費` です（`GuildManager.cs`）。
**冒険者1人ぶんの維持費はレベルと認定ランクだけで決まり、マスタには書きません**
（かつて `adventurers.json` に `upkeepGold` がありましたが、読まれるだけで使われていなかったので廃止しました）。
`rarity` は雇入れ費にだけ効き、**維持費には関係させません**（希少さは入手のしにくさであって、賃金の高さではないため）。
**レベルとランクに比例して増える**ので、報酬もレベル帯に比例させないと後半で破産します。
下の帯は「所属冒険者6人・帯の中央レベル・全員がその認定ランク」で見積もった賃金です。
施設の `upkeepGoldPerTurn` はこれとは別に積み上がります（全施設を建てると +233G/T）。
所属人数は同時に遠征できる編成人数とは別で、初期5人・宿舎をすべて建てて最大9人です。

雇入れ費は `(max(10, Lv × 55) + レアリティ上乗せ) × 1.5`（端数四捨五入）です
（`RecruitScreen.CalcHireCost`）。倍率は `RecruitScreen.HireCostRatePercent` の1か所だけで持ち、
Lv単価にもレアリティ上乗せにも同じように掛かります（Lv1のCommonで83G）。

| レアリティ | 上乗せ | Lv8での上乗せ | Lv8の雇入れ費 |
| --- | --- | ---: | ---: |
| Common | 0 | 0 | 660G |
| Uncommon | `20 + Lv × 1` | 28 | 702G |
| Rare | `40 + Lv × 2` | 56 | 744G |
| Unique | `70 + Lv × 3` | 94 | 801G |
| Legend | `110 + Lv × 4` | 142 | 873G |

**5段階すべてを `RecruitScreen.RarityHirePremium` に書きます。** 書き漏らしたレアリティは
switch の既定で0へ落ち、**希少なほど安い**という逆転になります。実際 Unique と Legend が
抜けていたせいで、Uniqueのファリンが Rare のライオスより安く雇えていました。
`recruitWeight`（出にくさ）とこの表は必ず同じ向きに並べてください。
在籍上限で人数を絞るぶん、1人あたりの雇入れ費を重くしてあります。

| ランク | F | E | D | C | B | A | S |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 冒険者の賃金の目安（G/ターン） | 30 | 90 | 160 | 240 | 325 | 410 | 500 |
| `rewardGold` | 100〜300 | 600〜1,500 | 1,500〜3,500 | 3,000〜6,500 | 5,000〜11,000 | 8,000〜16,000 | 12,000〜22,000 |
| `rewardGuildPoints` | 8〜80 | 15〜120 | 25〜150 | 40〜220 | 60〜300 | 80〜400 | 100〜500 |

### 敵の帯 (`enemies.json`)

`threat` は能力値に影響しません。**強さはこの表の数値がすべて**です。

| `threat` | HP | AV | DV | PV | ダメージダイス | `exp` |
| --- | --- | --- | --- | --- | --- | --- |
| 1 (F) | 7〜30 | 0〜2 | 2〜9 | -1〜3 | 1d3〜1d4+1 | 3〜6 |
| 2 (E) | 20〜42 | 0〜5 | 4〜6 | 0〜4 | 1d2〜1d6 | 7〜10 |
| 3 (D) | 25〜70 | 0〜8 | 3〜6 | 0〜6 | 1d2〜1d6+1 | 11〜16 |
| 4 (C) | 32〜90 | 1〜12 | 0〜8 | 1〜7 | 1d2〜2d4 | 16〜24 |
| 5 (B) | 50〜120 | 2〜14 | 3〜10 | 2〜9 | 1d4〜2d6 | 28〜30 |
| 6 (A) | 80〜160 | 3〜16 | 4〜11 | 3〜11 | 1d6〜2d8 | 34〜38 |
| 7 (S) | 120〜220 | 4〜18 | 5〜12 | 4〜13 | 1d8〜3d6 | 45〜70 |

`exp` の列は前掲「経験値」の表と同じ値です（`RankBandTable._enemies` が正）。

HP は `(vitality × 10 + constitution × 5) ÷ 2` から決まります。
AVの上端は冒険者側のPV上限（下記）と噛み合わせてあり、**先にPV上限を動かさずにAVだけ上げてはいけません**。

各帯に用意する敵の役割の内訳は次のとおりです。

| `threat` | 前衛（高AV低DV） | 遊撃（高DV低AV） | 後衛（射手・術者） | ボス級 |
| --- | --- | --- | --- | --- |
| 2 (E) | 9 | 4 | 4 | D帯から借りる |
| 5 (B) | 1 | 1 | 1 | 1 |
| 6 (A) | 1 | 1 | 2 | 1 |
| 7 (S) | 2 | 2 | 2 | 2 |

**E帯のボスはD帯の個体で表します。** F帯の昇格試験が老樹人(C)だったのと同じ考え方で、
帯の中に閉じたボスを置くより、一段上の個体を1体だけ据えるほうが「格上と当たっている」感触が出ます。
ルプス＝ポイズンファング・ランポス・アルファ・レッサーワイバーンがこれにあたります。

### E帯の顔ぶれ

Fがチュートリアルなので、**E帯が「噛み合わせを考えないと勝てなくなる」最初の段**です。
そのため、殴り合いの数値だけでなく**状態異常と装甲破壊**をここで初めて常設します。

| 舞台 | 前衛 | 遊撃 | 後衛 | 一段上（ボス） |
| --- | --- | --- | --- | --- |
| 森 | ポイズンマルフィサ | メイジルプス | — | ルプス＝ポイズンファング |
| 森（暗部） | スケルトン・ワイト | — | — | アイスグール(D) |
| 草原深部 | アシッドスライム | ランポス | — | ランポス・アルファ / レッサーワイバーン(D) |
| 天城低層 | 野盗・野盗の用心棒・脱走兵 | 野盗の追い剥ぎ・狩り鷹 | 野盗の呪い師 | 野盗頭(A) |
| 森・天城低層（ゴブリン） | ゴブリン兵士 | — | ゴブリン射手・ゴブリン魔導士 | ゴブリン頭目(C) |
| 廃坑 | 岩喰い虫 | — | 胞子茸人 | 岩喰いの女王蟲(D) |

**コボルトは廃止しました。** 犬型の亜人は冒険者側のヴァルグと役割が重なるためです。
廃坑のコボルト3種は岩喰い虫・胞子茸人・岩喰いの女王蟲に置き換えてあります。

### 魔法攻撃敵の分布

E以上は各ランクに魔法攻撃敵を最低2種類置きます。敵数は `EnemyData.IsMagicAttack`、
ユニット数は「魔法攻撃敵を1体以上含み、編成内の最大 `threat` がそのランク」の基準です。

| ランク | 魔法敵 | 魔法ユニット | 追加した代表例 |
| --- | ---: | ---: | --- |
| E | 4 | 8 | 灯火のウィスプ |
| D | 2 | 6 | 風哭きのハーピー |
| C | 3 | 6 | 天城の雷導機兵 |
| B | 2 | 4 | 冥府の託宣者 |
| A | 2 | 5 | 星嵐のセイレーン |
| S | 2 | 4 | 虚空竜 |

単体の術者だけでなく、前衛の背後から魔法を撃つ混成ユニットも各舞台へ配置します。
遭遇表では低めの重みにし、同じランクの通常敵編成が消えないようにします。

追加した6体はそれぞれ固有装備を落とします。既存ドロップで1種類しかなかった槍・火杖・風杖を
補い、守備側は1種類しかなかった盾と、少なかった頭防具へ振り分けて武器3／防具3に揃えます。

| ランク | 敵 | 固有ドロップ | 装備カテゴリ |
| --- | --- | --- | --- |
| E | 灯火のウィスプ | 灯火精の火杖 | 火魔法・両手武器 |
| D | 風哭きのハーピー | 風哭きの羽槍 | 槍・片手武器 |
| C | 天城の雷導機兵 | 雷導機兵の蓄電盾 | 軽装系・盾 |
| B | 冥府の託宣者 | 冥府の託宣頭巾 | 布・頭防具 |
| A | 星嵐のセイレーン | 星嵐の歌杖 | 風魔法・両手武器 |
| S | 虚空竜 | 虚空竜の角兜 | 重装・頭防具 |

### 装備の帯 (`equipment.json`)

剣（片手）を基準線にし、他の武器種はそこからの相対で決めます。
個性5項目と `maxStatBonus` は全Tier共通なので、**Tierで動かすのは `basePv` とダメージダイスだけ**です。

| `shopTier` | 1 | 2 | 3 | 4 | 5 |
| --- | --- | --- | --- | --- | --- |
| 剣(片手) `basePv` / ダイス | 3 / 1d3 | 4〜5 / 1d4 | 6 / 1d6 | 7 / 1d6+1 | 8 / 1d8 |
| 剣(両手) `basePv` / ダイス | 4 / 1d4 | 5 / 1d6 | 7 / 1d8 | 9 / 2d4 | 10 / 2d6 |
| 剣(片手) 価格 | 30 | 90 | 220 | ドロップ専用 | ドロップ専用 |

他武器種の剣との `basePv` 差は現状を踏襲します（短剣 -2 / 槍 +1〜+2 / 斧 -1〜-2 / 弓 ±0）。
Tier4の両手剣は既存の `eq_drop_bone`（`basePv` 9 / 2d4）を基準として採用します。

**ドロップ専用装備（ID が `eq_drop_` で始まるもの）は全武器種に1本以上置きます。**
剣ばかりが落ちると、剣以外を振る冒険者にとってドロップが「売るだけの物」になり、
敵を狩る動機が痩せます。武器種ごとの個性（`maxStatBonus` と `armorPierce` などの
`WeaponTraits`）は商店の同武器種・同持ち手と必ず同じ値にし、
ドロップらしさは `basePv`・ダメージダイス・`bonus` で出します
（この一致は `MasterValidator` がエラーとして見ます）。

これにより**冒険者のPV上限**は次のようになります。AVの帯はこれに合わせてあります。

| | 片手剣 | 両手剣 | 両手槍 |
| --- | --- | --- | --- |
| Tier3（商店の上限） | 12 | 13 | 15 |
| Tier5（ドロップ最上位） | 14 | 16 | 18 |

防具と盾の帯は次のとおりです。

| `shopTier` | Body の `av` | Body の `dv` | Head の `av` | 盾の `blockChance` / `blockAv` |
| --- | --- | --- | --- | --- |
| 1 | 0〜3 | -4〜+1 | 0〜1 | 25〜40% / 4〜7 |
| 2 | 1〜4 | -4〜0 | 2 | 30〜45% / 6〜10 |
| 3 | 5〜6 | -4〜0 | 3 | 35〜50% / 8〜12 |
| 4 | 7 | -5〜0 | 4 | 40〜55% / 10〜14 |
| 5 | 8〜9 | -5〜0 | 5 | 45〜60% / 12〜16 |

### 昇格

冒険者とギルドで昇格の仕組みが違います。

- **冒険者**: 自分のランクより**上**のクエストを正規クリアした回数で上がります
- **ギルド**: 緊急クエスト（`isEmergencyQuest` かつ `rankUpOnClear` > 0）のクリアで上がります

ギルドの昇格試験は各段に1本ずつ、計6本あります。
`requiredGuildPoints` は累積GPではなく、**現在のギルドランクになってから獲得したGP**と比較します。
昇格時にこのランク内GPだけが0へ戻るため、昇格試験の報酬で次の試験が即時解禁されることはありません。

| ギルド | F→E | E→D | D→C | C→B | B→A | A→S |
| --- | --- | --- | --- | --- | --- | --- |
| クエスト | `quest_promotion_1` | `quest_promotion_2` | `quest_promotion_3` | `quest_promotion_4` | `quest_promotion_5` | `quest_promotion_6` |
| クエスト `rank` | 1 | 2 | 3 | 4 | 5 | 6 |
| `requiredGuildPoints` | 50 | 200 | 500 | 900 | 1,500 | 2,400 |

## 敵の強さ (`enemies.json`)

**敵にレベルはありません。** マスタに書いた能力値がそのまま戦闘値になります。
かつては `level` の倍率で全能力を一律に伸ばしていましたが、それでは
「硬いが弱い」「脆いが痛い」を作れないため廃止しました。

個体差を壊さず全体の緊張感を保つため、戦闘へ投入するときだけ共通の圧力補正
（HP×1.35、命中+2、PV/mPV+1）を加えます。調整値は `UnitCalculator` に集約しています。

強さの段階は**別々の個体**で表します。

```
はぐれゴブリン(F) → ゴブリン(F) → ゴブリン投擲兵(F) → ゴブリン兵士(E) → ゴブリン重装歩兵(C)
```

- `enemies.json` の `threat`: 脅威度（1〜7 ＝ F〜S）。冒険者ランクと同じ物差しです。
  **能力値には一切影響しません。** 使い道は次の2つだけです
  - 遭遇時の士気ショック（敵の平均脅威度 − 味方の平均ランク、1段につき
    `MoraleState.ThreatGapFlat`(=20)、上限60）
  - クエストボードの難易度表示
- `enemies.json` の `defaultOffHandId` / `defaultShieldId`: 敵にも二刀流と盾を持たせられます。
  両手武器を持たせているときは使えません（`MasterValidator` が検査）
- `enemies.json` の `naturalAttackKind`: 牙や爪そのものの攻撃種別（既定は `Physical`）。
  `Magic` にすると、武器を持たないまま魔法攻撃で殴ってきます。判定は相手のmAVと
  こちらのmPVで行われ、貫通値に乗る能力値もSTRからINTへ変わります。
  **武器を持たせている敵では無視されます**（武器側の `attackKind` が優先）。
  メイジルプス・アイスグールのような「魔力を帯びた獣」を、装備を配らずに作るための項目です

**深部ほど強い敵を出したいときは、`dungeons.json` の `encounterTable` にある
`minPhase` / `maxPhase` を使います。** 強い敵ユニットを深いエリアだけに置けば、
奥へ進むほど手強くなります。倍率でのスケーリングはもうありません
（`enemyLevelPerPhase` と `enemy_units.json` の `baseLevel` は廃止されました）。

## 冒険者の成長

レベルアップで伸びるのは**1レベルにつき1能力だけ**（`AdventurerData.StatPointsPerLevel`）です。
レベルを重ねるだけで強くなると、キャラクターがいくらでも代替可能になってしまうためです。

どの能力が伸びるかは種族とクラスの重みつき抽選で決まり、プレイヤーには選べません。

```
重み = 0.2（全能力共通の下駄） + 種族の成長率 + クラスの成長率   （負の重みは0に切り上げ）
```

- 下駄があるので、不得手な能力もときどき伸びます。完全に固定すると育ちの個体差が消えます
- 体格(SIZ)は成長対象外です。素の装甲AVと積載上限は雇用時の素質で決まります
- 施設の `growthRateBonusPercent` は「その確率でもう1点」として効きます

恒久的な能力の増減は、このほかに**クエスト中の賭けイベント**（次節）と
**特性**（後述「特性 (`traits.json`)」）から起きます。前者は外から降ってくる幸運や呪い、
後者はその冒険者自身の戦い方から生えるもので、担っている役割が違います。

## 道中の購入イベント (`Purchase`)

`choice_events.json` の選択肢へ `"effectType": "Purchase"` を置くと、遠征中の
商人から装備または消耗品を1個購入できます。

- `targetId`: `equipment.json` または `consumables.json` のID
- `value`: ランク倍率を掛けない提示価格（Gold）
- 代金は選択時に即時支払い、商品は `pendingLoot` に積まれて帰還時に倉庫へ入ります。
  全滅すると商品は失いますが、支払った代金は戻りません
- パーティの報酬Gold補正を交渉力として逆向きに使います。
  `実価格 = ceil(提示価格 × (100 - Gold補正) / 100)`、補正は -25%〜+25% で止まります
- 資金不足なら選択は確定しません。購入イベントには、情報を聞く・見送るなど
  **資金を使わない選択肢を必ず1つ以上**置いてください
- 購入後に0Gになる取引も破産回避のため確定しません。最低1Gは残す必要があります
- `Purchase` はランダムな `outcomes` には置けません。数量は常に1個です

`Gold` に負の `value` を指定した場合も、その場で資金から支払われます。通常の選択肢は
資金不足または残金0Gになる場合に確定せず、別の選択肢を選べます。ランダムな `outcomes` の
強奪・紛失は結果の引き直しを防ぐため必ず確定し、最低1Gを残して所持金の範囲だけ失います。
正の `Gold` は従来どおり `pendingLoot` に積まれ、帰還時に加算されます。

```json
{
  "text": "罠外し道具を買う",
  "resultText": "隊商主が道具一式を取り出した。",
  "effectType": "Purchase",
  "value": 30,
  "targetId": "item_trap_kit"
}
```

## 賭けの選択イベント (`choice_events.json`)

選択肢に結果テーブルを持たせると、「運が良ければ強くなる」出来事を作れます。

- `targetsOneMember`: `true` にすると、実行前にプレイヤーが隊員を1人選びます。
  **対象を決めてから結果を抽選する**ので、プレイヤーは「誰に賭けるか」だけを決めます
- `outcomes`: 重み付きの結果表。空なら従来どおり選択肢の `effectType` がそのまま起きます。
  2件以上あるとゲーム内で「何が起きるかは分からない」と表示されます

対象1人に効く `effectType` は次の4つです。使うには `targetsOneMember` が必須です。

| effectType | 効果 | `targetId` |
| --- | --- | --- |
| `AdventurerStatUp` | 能力を恒久的に +`value` | 能力名（`Strength` など）。空ならランダム |
| `AdventurerStatDown` | 能力を恒久的に -`value`（下限1） | 同上 |
| `AdventurerSkill` | スキルを習得させる | スキルID |
| `AdventurerDamage` | 最大HPの `value`% ダメージ（HP1で止まる） | 使わない |

```json
{
  "text": "隊員の一人に手を触れさせる",
  "resultText": "窪みに手を差し入れた。",
  "effectType": "None", "value": 0, "targetsOneMember": true,
  "outcomes": [
    { "weight": 30, "effectType": "AdventurerStatUp",   "value": 1, "resultText": "温かい光が腕を這い上がった。" },
    { "weight": 25, "effectType": "AdventurerStatDown", "value": 1, "resultText": "何かを吸い取られた気がする。" },
    { "weight": 20, "effectType": "AdventurerDamage",   "value": 25, "resultText": "針のような衝撃が走った。" }
  ]
}
```

恒久的な能力減少を序盤で踏ませたくないので、賭けイベントは奥地のダンジョンにだけ
割り当てています（`dungeons.json` の `turnEndEventIds`）。**どのダンジョンからも
参照されていないイベントは一度も発生しません。**

## 特性 (`traits.json`)

**その冒険者が実際にどう戦ってきたか**から生える恒久的な変化です。レベルアップの能力成長は
種族・職業の抽選で決まりプレイヤーには選べませんが、特性は**プレイヤーが選びます**。
育成の主導権は、能力値の振り分けではなくこちらのチャンネルで返しています。

効果の実体は `skills.json` のスキルで、`traits.json` は**解禁条件と見せ方だけ**を持ちます。
こうするとスキルの表現力（`add` / `mul` / `battle` / `expedition`）がそのまま使え、
習得も戦闘への反映もセーブも既存の経路に乗ります。

```json
{
  "id": "trait_unyielding",
  "traitName": "不屈",
  "skillId": "skill_trait_unyielding",
  "description": "追い詰められるほど攻撃が重くなる",
  "awakenText": "は死線を越えた",
  "flavorText": "膝は、折れなかった。",
  "requirements": [
    { "record": "NearDeathRounds", "atLeast": 8 }
  ]
}
```

### 代償は先払い

**特性は原則として諸刃**で、利点と欠点を併せ持ちます。欠点のない素直な強化を出してよいのは、
**リスク記録を解禁条件に含めているときだけ**です。

| | 諸刃 | 純粋強化 |
| --- | --- | --- |
| 欠点 | 必ず持つ | なし |
| 解禁条件 | 通常記録だけでよい | **リスク記録が必須** |
| 例 | 殺し慣れ（PV+1／士気×0.9） | 不屈（背水PV+2、代償なし） |

欠点の有無は**フラグではなくスキルの数値そのものから導いています**（`TraitAnalysis`）。
「欠点を書き忘れたまま純粋強化になっていた」というずれが起きないようにするためで、
`MasterValidator` がこの規則を検査し、破ると**エラー**になります。

なお `threatWeight` の + と、`trapChancePercent` / `enemyEncounterChancePercent` の + は
担い手にとって不利なので、欠点として数えます。

### 担い手の型 (`builds`)

**この規則は型ごとに成り立っていなければなりません。** 戦闘の計算は物理と魔法で経路が分かれていて、
`pv` は魔法攻撃に乗らず、`mpv` は物理攻撃に乗りません。回復役はそもそも攻撃判定を通りません。
つまり**同じ数値でも、誰が持つかで意味がまるで変わります**。

| 型 | 生きている数値 | 死んでいる数値 |
| --- | --- | --- |
| `Physical` | ほぼ全部（盾も持てる） | `mpv` / `heal` / `cleanseOnHealChancePercent` |
| `Magic` | `mpv`・命中・会心・急所突き・守り | `pv` / `armorPierce` / `armorShred` / 背水PV / 弱った敵へのPV / 盾関連 / 回復関連 |
| `Heal` | `heal`・浄化率・回復時士気・守り | 攻撃に関わる数値すべて / 盾関連 |

これを踏まえないと、次の2つの壊れ方をします。**実際に両方やらかしました。**

- **代償がタダになる** — 「引き際を知る」の `pv -1` は術者には何も奪わないので、
  リスクを払わない純粋強化が成立してしまう
- **利点だけが消える** — 「虚ろな目」の `afflictedTargetPv` は術者には効かないので、
  士気の代償だけが残る純粋な罰則になる

`traits.json` の `builds` で「誰に出す特性か」を宣言します（省略＝全型）。
`MasterValidator` は**宣言した型それぞれについて**、利点が1つも残らないもの、
代償がないのにリスクを要求していないものをエラーにします。

```json
"builds": ["Physical", "Magic"]
```

物理専用の数値を書いたなら `["Physical"]` を宣言してください。全型向けに出したい特性は、
HP・士気・AV・mAV・DV・応急処置・狙われやすさ・遠征効果のような**型を選ばない数値**で組みます。

候補の絞り込みは**いま構えている得物**で行います（`TraitSystem.LensOf`）。
杖に持ち替えた戦士には物理特性が出なくなりますが、**すでに身につけた特性は失われません**
（覚えたスキルが職業を変えても残るのと同じ扱い）。

### 遠征記録 (`ExpeditionRecordType`)

解禁条件に書ける記録です。数え方の原則は「**結果ではなく身の置き方を数える**」こと。
倒した数だけを数えると強い隊員がさらに強くなるだけなので、削られながら立っていた時間や、
弾かれ続けた回数のような「うまくいかなかった経験」も同じ重みで数えます。

記録には**粒度の違う2つの供給源**があります。

**戦闘ラウンド単位**（`BattleResolver` のフックが数える）

| 記録 | 種別 | 数えるもの |
| --- | --- | --- |
| `NearDeathRounds` | **リスク** | HPが4分の1以下でラウンドを迎えた回数 |
| `TimesDowned` | **リスク** | 戦闘不能になった回数 |
| `AlliesFellBeside` | **リスク** | 目の前で味方が戦闘不能になった回数 |
| `LowHpRounds` | 通常 | HPが半分以下でラウンドを迎えた回数 |
| `Kills` | 通常 | とどめを刺した回数 |
| `CritKills` | 通常 | 会心でとどめを刺した回数 |
| `ProtectedAlly` | 通常 | 庇護に成功した回数 |
| `ShieldBlocks` | 通常 | 盾で受け止めた回数 |
| `RepelledByArmor` | 通常 | 自分の攻撃が1回も貫通せず弾かれた回数 |

**依頼1本の結末**（`ExpeditionOutcomeRecorder` が数える）

| 記録 | 種別 | 数えるもの |
| --- | --- | --- |
| `QuestsFailed` | **リスク** | 全滅した遠征に加わっていた回数 |
| `ComradesLost` | **リスク** | 連れて帰れなかった仲間の人数（戦闘不能ではなく実際の死） |
| `SoleSurvivor` | **リスク** | 自分だけが生きて帰った回数 |
| `SoloClears` | 通常 | 単独でクエストを正規クリアした回数 |
| `BossKills` | 通常 | ボス編成の最後の1体へとどめを刺し、その遠征から生還した回数（同行者全員には加算しない） |
| `Retreats` | 通常 | 撤退して帰った回数 |
| `FlawlessClears` | 通常 | 誰ひとり戦闘不能にならずにクリアした回数 |

**失敗と喪失もリスク記録**です。全滅を潜ったことや仲間を失ったことは、瀕死で立ち続けたことと
同じかそれ以上に高くついた経験なので、素直な見返り（純粋強化）の代価として認めています。

`ExpeditionOutcomeRecorder.Record` は**必ず帰還時の死亡判定より後に呼びます**。
戦闘不能は即死ではなく死亡が確定するのは `ResolvePendingTrauma` のあとなので、
順序を入れ替えると `ComradesLost` が常に0になります。
`FlawlessClears` は帰還時の状態からは判定できない（戦闘不能が負傷へ解決済みのため）ので、
同じ遠征の `TimesDowned` が全員0かどうかで導いています。

記録は**冒険者の生涯にわたって累積**します。撤退でも全滅でも数えます
（依頼を果たせたかどうかと、どう戦ったかは別の話なので）。
セーブデータに数値で載るため、**既存の列挙値を振り直してはいけません**。追加は末尾に付けます。

### 提示のされ方

クエスト帰還時に、条件を満たした特性が**隊員1人につき最大1件、最大3候補**で提示されます。
リスク記録を要求する特性が先頭に来ます。

同じ節目の選択肢を必ずまとめて見せる場合は、各特性へ同じ `offerGroup` を指定します。
グループは3候補の上限をまたいで分割されず、残り枠へ収まらなければ次回へ送られます。
1つを選ぶか見送ると、そのグループの候補はすべて提示済みになります。

**身につけられるのは1つだけで、選ばなかった候補は二度と現れません。** 見送っても同じです。
毎クエスト同じ問いを繰り返さないためと、その場の選択に重みを持たせるための両方です。

解禁条件は職業スキルの必要習熟度と同じように、ヘルプの「特性と解禁条件」
（`H` → `9`）へマスタから組み立てて並べます。**担い手の型ごとに節を分け**、
効果と代償はその型から見た値で出すので、術者の欄に物理専用の数値が並ぶことはありません。
`traits.json` に足した特性は自動でここに載ります。

### 数値の決め方

閾値は**実際にクエストを走らせて測ってから**決めます。旧仕様のF帯4人で5本走らせた実測は、
とどめ6〜13／手傷ラウンド3〜8／瀕死ラウンド2〜8／会心とどめ0〜1でした。
現在のF帯は3人編成が基準なので、次回の閾値調整では3人編成の実測を使います。
会心とどめのように**滅多に起きない記録に大きな閾値を置くと、その特性は事実上到達不能**になります。

`family` は**特性ごとに固有の名前空間**にしてください。職業マスタリーと共有すると、
同じfamilyは最上位1つしか効かない仕様のせいで**特性がマスタリーを黙って押しのけます**。
`MasterValidator` がこの衝突も検査します。

## スキル (`skills.json`)

スキルは、装備条件つきの能力補正、遠征全体への効果、戦闘中の条件発動を表します。
冒険者も敵も同じ表を引きます。

### 段階スキル (`family` / `level`)

同系統のスキルは `family` で束ね、`level` で段階を表します（Lv1〜Lv5）。

**同じ `family` の中では、覚えているうち `level` がいちばん大きい1つだけが効きます。**
下位の段階は押しのけられて数値が積み上がらないので、各段階の `add` / `mul` には
**その段階での最終的な強さをそのまま書きます**（差分ではありません）。
畳み込みは `SkillProgression.Collapse` が行い、冒険者の `Skills` と敵の `Skills` の
両方に効きます。`family` が空のスキルは単独扱いで、常にそのまま乗ります。

`level` を飛ばして与えても構いません（Lv1 の次に Lv3 を渡せば、Lv2 は素通しで Lv3 が効く）。

### 名簿から持ち込むユニークスキル

`adventurers.json` の `skillIds` に書いたスキルは、その冒険者を雇った瞬間から効きます。
職業マスタリーと違って習熟度を要求せず、職業を変えても外れないので、
**その人にしかできないこと**を1つだけ渡す枠として使います。

- `classes.json` の `classSkills` には載せない（載せると誰でも取れてしまい、固有ではなくなる）
- `family` は空にする。段階スキルの束に混ぜると、マスタリー側と押しのけ合いになる
- 効果は職業の看板と噛み合う条件を付ける。神官のミカヤが持つ「銀色の髪の乙女」
  （`skill_unique_silverMaiden`）は光魔法の杖を握っているあいだだけ、回復量を1.1倍にし、
  手当てが通るたびに士気を最大値の2%だけ戻します
- **複数人で分け合うスキルは装備条件を付けない。** 地下大迷宮の5人が持つ「モンスター食」
  （`skill_unique_monsterCuisine`）は職業も得物もばらばらな5人に跨るので、
  握っている物では条件を書けません。代わりに「同じ心得を持つ仲間が増えるほど効く」形にし、
  同席する人数そのものを条件にしています

### 有効になる条件

| フィールド | 条件 |
| --- | --- |
| `frontOnly` / `backOnly` | 前衛（スロット0〜2）／後衛（3〜5）にいるとき |
| `requireWeaponType` + `requiredWeaponType` | その武器種を右手に握っているとき |
| `requireArmorType` + `requiredArmorType` | その防具種を胴に着ているとき |
| `requireUnarmed` | 右手に何も握っていないとき（格闘術） |
| `requireTwoHanded` | 両手武器を構えているとき |
| `requirePhysicalWeapon` | 物理武器を構えているとき（杖を除きたい両手武器術で併用） |
| `requireShield` | 左手に盾を構えているとき |
| `requireOffHandWeapon` | 左手に武器を握っているとき（二刀流） |

判定は `UnitCalculator.MeetsGearRequirements` にまとまっています。
立ち位置を見ない条件だけを切り出してあるので、積載や素手ダメージのように
隊列を組む前に決まる値からも同じ判定を使えます。

### `add` に書ける項目

従来の `hp` `san` `av` `mav` `pv` `mpv` `dv` `toHit` `heal` `armorPierce`
`armorShred` `critRange` `extraAttacks` `offHandChance` `blockChance` に加えて、

| 項目 | 意味 |
| --- | --- |
| `blockNegate` | 盾で受けたとき、相手のPVに関わらずダメージを丸ごと消す確率（%） |
| `carry` | 積載上限への加算。重い装備の過積載ペナルティを打ち消す |
| `threatWeight` | 狙われやすさ（%）。+で囮、-で的にされにくい。下限は素の重みの10% |
| `autoPenetrate` | 貫通が1回も出なかったときに、装甲を無視して1貫通を拾う確率（%） |
| `critPv` | 会心したときに上乗せするPV（会心の効きそのものを重くする） |
| `emergencyHeal` | HPが半分を切った瞬間に最大HPのこの%を回復（1戦闘に1度きり） |
| `incomingCritChancePercent` | **この者へ当たった一撃が会心へ跳ね上がる確率（%）。持ち主にとっての欠点** |

`mul` は従来どおり `hp` `san` `heal` の3つだけです。AV/DV/PVは1点の重みが大きいので倍率では触りません。

`unarmedDamageDice` は素手のダメージダイスの差し替えです（例 `"1d6"`）。
複数持っていても最大出目がいちばん大きい1本だけが採用されます。

### 遠征に効く効果 (`expedition`)

戦闘の数値ではなく「持ち帰るもの」と「道中で起きること」に効きます。

| 項目 | 意味 |
| --- | --- |
| `goldPercent` | クエスト報酬のゴールドへの増減（%） |
| `expPercent` | 経験値への増減（%）。クエスト報酬と戦闘報酬の両方に効く |
| `treasureChancePercent` | ダンジョンの宝箱イベントの出やすさへの増減（%） |
| `trapChancePercent` | 罠イベントの出やすさへの増減（%）。負で踏みにくくなる |
| `enemyEncounterChancePercent` | 敵遭遇イベントの出やすさへの増減（%）。斥候術は負の値 |
| `healEventChancePercent` | 休息イベントの出やすさへの増減（%） |
| `restHealPercent` | 休息イベントで回復するHPへの増減（%） |
| `enemyDropChancePercent` | 敵ごとのドロップ抽選率への増減（%） |
| `rareDropChancePercent` | 装備のCommon超過1段階ごとに加える抽選率（%）。品物自体のレアリティは変えない |
| `postBattleHealPercent` | 戦闘に勝ったあと、生き残った隊員が最大HPのこの割合（%）だけ回復する |
| `postBattleHealPerCompanionPercent` | 同じ効果を持つ隊員が1人増えるごとに上の値へ加える割合（%） |
| `phasesPerTurnBonus` | 1ターンに踏み込むエリア数への加算（%ではなくエリア数）。幌馬車系がこれを持つ |

**パーティ全員ぶんが合算されます**（`PartySkillEffects`）。隊列も生死も関係なく、
その遠征に誰を出したかだけで決まります。ゴールドと経験値の減少は -100% で下げ止まります。

**戦闘後回復だけは合算しません。** いちばん高い `postBattleHealPercent` を基準にして、
その効果の持ち主が1人増えるごとに `postBattleHealPerCompanionPercent` を上乗せします。
素直に足し合わせると4人で4倍になり、「みんなで囲む食卓」ではなく「4回食べる」に
なってしまうためです。上限は `PartySkillEffects.MaxPostBattleHealPercent`（=25%）。
回復するのは戦闘に勝った直後で、対象は**戦えるまま戦闘を終えた隊員だけ**です
（倒れた者は食卓に着けません）。休息イベントと違って場所を選ばず、勝つたびに
少しずつ返ってくるのが持ち味になります。

`phasesPerTurnBonus` は `quests.json` の `phasesPerTurn`（全帯5）に足されます。
**`totalPhases` は変わらない**ので、道中のイベントを飛ばすのではなく、同じ行程が
少ないターンで終わります（例: 全42エリアなら 9ターン → +1で7ターン）。
合計は `PartySkillEffects.MaxPhasesPerTurnBonus`（+5）で頭打ちになり、
どれだけ足を引っ張られても1ターンに1エリアは必ず進みます。
幌馬車 Lv5（+5）だけで頭打ちに届くので、馬車は隊に1台あれば足ります。
採取クエストの延長で伸びる行程も、この「1ターンぶんの行軍」と同じ値になります。

イベント確率は `dungeons.json` の `eventTable` の重みに掛かるので、
**そのダンジョンで重みが0のイベントはスキルでも生やせません**。

### 戦闘中に条件発動する効果 (`battle`)

| 項目 | 意味 |
| --- | --- |
| `protectAllyHpPercent` | このHP率以下の味方が狙われたとき庇護を試みる。0なら無効 |
| `protectChancePercent` | 庇護して攻撃対象を自分へ変える確率（%） |
| `afflictedTargetPv` | 毒・出血・火傷中の敵に対する物理PV加算 |
| `cleanseOnHealChancePercent` | 回復成功時、対象の有害状態を1つ解除する確率（%） |
| `moraleOnHealPercent` | 回復成功時、パーティの士気を最大値のこの割合（%）だけ戻す |
| `lowHpThresholdPercent` | 背水効果が有効になる自分のHP率（%） |
| `lowHpPv` | 背水条件中の物理PV加算 |
| `counterChancePercent` | 盾でダメージを完全に防いだ直後、主武器で反撃する確率（%） |

`battle` の効果にも前衛・後衛・装備条件が適用されます。庇護と反撃は盾、浄化と回復時の士気は光の杖、
処刑人と背水は物理武器を条件にしてあります。反撃から連撃・左手攻撃・再反撃は発生しません。

`moraleOnHealPercent` は**手当てが実際に通ったときだけ**効きます。士気の最大値は編成の san 合計で
決まるので、固定値ではなく割合で持たせて編成規模に依らない効きにしてあります。同じ効果を
複数のスキルから持っていても、いちばん高い1つだけが効きます（浄化と同じ扱い）。
士気はパーティ側の指標なので、**敵の術者が味方を癒しても士気は動きません**。
手当ては味方が傷ついているときにしか起きないため、削られた直後にだけ少量が返る形になります。

## 職業スキルの解禁 (`classes.json`)

`classSkills` の `requiredClearCount` は、解禁に必要な「**クラス習熟度**」そのものです。
0 なら職業に就いた瞬間に手に入ります。名前は昔の名残で、値は習熟度（pt）そのものが入っています。

習熟度が入るのは、次をすべて満たしたときだけです（`AdventurerData.OnClearQuest`）。

- そのクエストに参加していた
- クエストを正規クリアした（撤退・全滅では増えない）
- 帰還時に生存していた
- **クエストが適正ランクだった**

1回の獲得量は `100 + clamp(INT, 0, 30)` です。通常の基礎値は100で、INT 1につき+1、
上限は130（基礎値の+30%）。INTが低くても基礎値を下回ることはありません。
たとえば INT 4 は104、INT 15 は115を獲得します。

適正ランクは「自分と同じランクから `Rank.SuitableRangeAbove`(=2) つ上まで」です。
格下は学ぶものがなく、格上すぎるクエストは連れ回されているだけなので、どちらも数えません。

| 冒険者ランク | F | E | D | C | B | A | S |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 適正クエスト | F〜D | E〜C | D〜B | C〜A | B〜S | A〜S | S |
| 習熟度上限 | 700 | 1,800 | 3,300 | 5,200 | 6,300 | 7,500 | 8,800 |

**冒険者ランクの昇格**は、「格上のクエストを正規クリアした回数」と
「これまでに正規クリアした適正クエストの累積回数」の**両方**が閾値に届いたときに解禁されます
（`AdventurerData.CanRankUp`）。同ランク以下は格上には数えませんが、
適正帯であれば累積には載ります。累積は昇格後もリセットされません。
条件を満たしても自動では上がらず、プレイヤーが冒険者詳細で「昇格させる」を選んだときに
`AdventurerData.TryRankUp` が呼ばれて実行されます。

| 昇格 | 必要な格上クリア | 累積の適正クリア |
| --- | --- | --- |
| F → E | 1 | 3 |
| E → D | 2 | 10 |
| D → C | 3 | 30 |
| C → B | 3 | 50 |
| B → A | 4 | 75 |
| A → S | 4 | 100 |

昇格1回あたりの報酬は一律で、**全能力値 +`AdventurerData.RankUpStatGain`（=2）** と
**現在の職業への習熟度 +`AdventurerData.RankUpMasteryGain`（=1000）**（およびそこで解禁される
スキル）です。維持費は既存の `GuildManager.CalculateAdventurerUpkeep` を通じて自動で上がり、
1ランクにつき恒久的に `GuildManager.UpkeepGoldPerRank`（=6G/T）増えます。報酬側をここまで
強くしてあるのは、この恒久コストに見合う一度きりの見返りにするためです。
昇格の内容は冒険者の遠征履歴とギルドの経済ログの双方に、スキル習得も含めた1行にまとめて記録されます。

習熟度は職業ごとに別勘定で、一度覚えたスキルは職業を変えても失われません。
元の職業に戻れば習熟度は続きから数え直しになるため、複数の職業を渡り歩いて
マスタリーを集めることもできます。

現在の13職業は、看板の武器・魔法・格闘マスタリーが Lv1→Lv5 まで伸びる12段の骨組みに、
防具マスタリー、持ち味のスキル、条件発動スキルを挟んであります。同じ習熟度で複数を
解禁する職業もあります。基準の解禁習熟度は
0 / 300 / 700 / 1200 / 1800 / 2500 / 3300 / 4200 / 5200 / 6300 / 7500 / 8800 です。
Lv5 は習熟度8,800と遠いので、**終盤の目標**として置いてある想定です。

同系統は最上位だけが効くので、`classSkills` に Lv1〜Lv5 を全部並べても二重には乗りません。
低いLvを並べるのは「育つ途中の姿」を作るためです。

**同じ理由で、`onHitStatuses` / `battleStartStatuses` は上位段にも必ず書き写してください。**
下のLvにしかない付与効果は、Lvを上げた瞬間に黙って消えます（弓マスタリーLv1の出血が
Lv2で消えていた実例があります）。`SkillProgressionTests` の
`ATierNeverLosesTheStatusEffectThatALowerTierAlreadyGranted` が全系統を見張っています。

なお `MasterLoader` はスキルIDが解決できない `classSkills` を黙って読み飛ばします。
ID を打ち間違えるとそのスキルは永久に手に入らないので、追加したら
「職業とマスタリー」ヘルプ（`dotnet run --project GuildSimulator.Cli` → `H` → `8`）に
並ぶことを確認してください。ヘルプはマスタから組み立てているので、
載っていなければ読み込めていません。

## ギルド施設 (`facilities.json`)

建設するとゴールドを消費し、以後は `upkeepGoldPerTurn` が毎ターンの維持費に加算される。
効果は建設した施設すべての合計値として常時適用される（`requiredGuildRank` はギルドランクによる建設条件）。

- `questBoardBonus`: クエスト掲示板の通常枠を増やす数
- `shopLevelBonus`: 商店の品揃えレベルを増やす数（`equipment.json` の `shopTier` 以下の装備が商店に並ぶようになる。
  基準の商店レベルは1 ＝ `ShopService.BaseShopLevel`）。
  `fac_shop_ext_01` と `fac_shop_ext_02` を両方建てると商店レベル3になり、物差しどおり `shopTier` 3 まで買えます。
  **ID が `eq_drop_` で始まる装備は `shopTier` に関係なく商店に並びません**（ドロップ専用の印）
  解禁済みの市販品は抽選・在庫切れなしで常備され、画面では武器・防具・装飾品を分けて表示します。
- `restHealBonusPercent`: 遠征中の休息回復量を増やす割合（%）
- `growthRateBonusPercent`: 冒険者のレベルアップ成長率を増やす割合（%）。1%単位で調整する想定
- `partySlotBonus`: 1つのパーティへ同時編成できる人数の加算値
- `rosterSlotBonus`: ギルドに在籍させられる冒険者の人数（在籍上限）の加算値

パーティの配置盤は常に前衛3枠・後衛3枠の6マスですが、**同時に配置できる人数**には
進行段階ごとの上限があります。人数上限以内なら、序盤から後衛を含む好きなマスへ配置できます。

| 段階 | 建設可能ランク | 施設 | 編成人数上限 |
| --- | --- | --- | --- |
| 基礎 | F | ― | 3人 |
| 強化1 | E | 小隊指揮所 | 4人 |
| 強化2 | C | 戦術通信室 | 5人 |
| 強化3 | A | 遠征司令部 | 6人 |

`partySlotBonus` の全施設合計は3以下にします。上限は新規クエストの出発時に検査し、
セーブ済みの進行中遠征は6マスのまま復元するため、既存遠征の隊員を切り捨てません。
旧セーブに新しい編成施設IDが無い場合も、次の新規出発からは3人上限を適用します。
高ランクの旧ギルドはE・C・Aの建設条件をすでに満たすため、資金を用意すれば3施設を順に建設できます。

### 在籍上限（`rosterSlotBonus`）

編成上限とは別に、**ギルドに抱えられる冒険者の総数**にも上限があります。
上限に達している間は雇入れができず、宿舎系の施設を建てるまで新しい冒険者を迎えられません。

| 段階 | 建設可能ランク | 施設 | 在籍上限 |
| --- | --- | --- | --- |
| 基礎 | F | ― | 5人 |
| 強化1 | F | 冒険者宿舎 | 6人 |
| 強化2 | E | 宿舎の増築 | 7人 |
| 強化3 | C | 冒険者寮 | 8人 |
| 強化4 | A | 大宿舎 | 9人 |

基礎の在籍上限は編成上限より2人多くしてあります。1回の全滅で手駒が尽きると、
負傷者の休養が明けるまで何もできないまま維持費だけが減っていくためです。

`rosterSlotBonus` の全施設合計はちょうど4（＝`GuildManager.MaximumRosterCapacity` − 初期5人）に
します。多すぎても少なすぎても `--validate-master` がエラーにします。
在籍人数は生存者だけを数えるので、埋葬待ちの故人が枠を塞ぐことはありません。

レアリティには `Common`、`Uncommon`、`Rare`、`Unique`、`Legend` を指定します。
省略時は装備がCommon、冒険者は採用ウェイトから既定値が設定されます。

編集後はリポジトリのルートで次を実行すると、JSON構文と主要なID参照を検証できます。

```powershell
dotnet run --project GuildSimulator.Cli -- --validate-master
```

出力は2段になっていて、意味が違います。

- **エラー** — 壊れているデータ。参照が解決できないIDや、矛盾した設定がここに出ます。
  1件でもあると終了コードが1になります。**`MasterLoader` は解決できないIDを黙って読み飛ばす**ため、
  ここで拾わないと「エラーは出ないがゲーム内に一生出てこない」データができます。
  検査対象は `classSkills.skillId` / `skillIds` / `formationIds` / `encounterTable.unitId` /
  `turnEndEventIds` / `dungeonId` / `bossEnemyId` です
- **ランク帯の逸脱** — 「ランク帯の物差し」の帯から外れている値と、
  まだ用意されていないランク帯。**エラーではありません**。帯は作りたいゲームの形を先に
  決めたものなので、C〜Sのデータを入れ終えるまでは既存データのほうが帯の外にいます。
  ここは**残りの作業量を数える表**として読み、0になったときがデータの揃ったときです

エラーが0件のときは、読み込んだ冒険者、装備、敵、消費アイテム、選択イベントの件数が表示されます。
