# Build output (debug only — release binaries are intentionally committed)
**/bin/Debug/
**/obj/

# テストプロジェクトは配布物ではないので、Release出力も含めて追跡しない。
GuildSimulator.Tests/bin/

# OS / editor
.DS_Store
*.user
.vs/
.vscode/
.idea/

# Temp / install files
*.deb
*.bak
packages-microsoft-prod.deb
